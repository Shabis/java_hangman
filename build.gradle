apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "word-game"
version = '1.0'
mainClassName = "WordGame"

repositories {
  mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}
